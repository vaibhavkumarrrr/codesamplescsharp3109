namespace csharp.training.congruent.apps
{
    internal class Program
    {
        // The default Hello World Application
        // Since args are not being used, 
        // we use the discard (_) operator to ignore them
        static void Main(string[] _)
        {
            // This code has no try-catch code 
            Console.WriteLine("Hello, World!");
            return; // if Main was declared as int, 
            // return 0 for success, any other 
            // number indicating error code.. .
        }
    }
}
